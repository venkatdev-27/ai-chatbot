import React from 'react';
import './Loader.css'; // Create CSS for loader

const Loader = () => {
    return (
        <div className="loader-container">
            <div className="spinner"></div>
        </div>
    );
};

export default Loader;
